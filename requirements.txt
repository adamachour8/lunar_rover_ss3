# requirements.txt — Mission lunaire SS3
# Installer avec : python -m pip install -r requirements.txt

# Perception & traitement nuage de points
numpy
pyransac3d
scikit-learn      # sklearn : DBSCAN, NearestNeighbors (pip install scikit-learn)
scipy             # Delaunay triangulation

# Visualisation
matplotlib

# Interface hardware
pyserial          # Communication série avec Arduino (import serial)

# Simulation / terrain
pygame            # Terrain generator (si utilisé)